pipeline {
    agent any
    stages {
        stage('Build") {
            steps {
                sh 'echo "Hello world"'
                sh '''
                    echo "Multi-line shells steps work too"
                    ls -lah
                    '''
            }
       }
    }
}  
